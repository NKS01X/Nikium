readInt = fn() {
    c = readline();

    l = len(c);
    sign = 1;
    i = 0;
    if c[0] == "-" {
        sign = -1;
        i = 1;
    }
    
    ans = 0;

    while i < l {
        ans = ans * 10 + (c[i] - "0");
        i = i + 1;
    }
    return sign *  ans;
};